# Changelog

本文件记录 Legal OS 公开预发布版本的主要变化。

## [v0.8.0] - 2026-09-03

状态：**已发布公开预发布版，非稳定版**。本节记录 v0.8.0 相对于已发布公开基线的适配变化。

### 现行法研究默认路由

- T-05 `current-law-research` 现默认执行 `cn-legal-research`；`case-research` 继续执行 `cn-case-hub`。
- `cn-law-hub` 保留为兼容/过渡 Skill，仅用于旧调用方的来源元数据、查询计划和 authority-record 校验，不再作为默认现行法入口。

### 独立公开适配

- 新增独立的 Python 标准库 `cn-legal-research`，覆盖十类登记官方来源的搜索、详情、预览、条文、下载、有限跨法规搜索、地域分类和可选 JSON-RPC 接口。
- 增加显式缓存、限速、重定向主机校验、下载路径约束、authority-record 校验和离线 mock/fixture 测试；公开包不包含法规数据、凭据、第三方 SDK 或复制的爬虫源代码。
- `NOTICE` 和 `OPEN_SOURCE_BOUNDARY.md` 说明官方来源内容、访问条件和第三方署名/许可边界；来源登记不是对官方数据库或内容的所有权声明。

### Office 候选模板

- 13 个公开法律 DOCX 模板随 v0.8.0 公开预发布包发布，并保留原始 DOCX 的 OOXML、文本、表格、编号、样式和其他包级特征；模板目录仍明确标记为 `candidate`，表示它们仍需独立的运行时启用/替换决定。

### 发布前验证

- 公开表面、仓库结构、路由、模板包/哈希、公开边界、标准库运行时和全仓测试均在本地发布前门中验证；详细命令与未执行的在线访问项见 [`TEST_REPORT_v0.8.0.md`](TEST_REPORT_v0.8.0.md)。

### 发布状态

- v0.8.0 已作为 GitHub 公开预发布版发布，Tag 为 `v0.8.0`；本版本不是稳定版。

### 公开首页视觉更新

- 更新 `legal-os-banner.png` 为极简法律海报风格：保留印章/印泥的压印质感和法律文书语义，减少功能图标与内部说明文字；当前文件 SHA-256 为 `22d21f0a6c1a06cc15c44d178b0ca2245b58b3ff8f167ddb2fa5b19ed2e78cac`。
- 本次只更新公开 `main` 的首页资产；`v0.7.0` tag 和其对应的历史发布包保持不变。

## v0.8.0 历史公开适配基线 - 2026-08-25

状态：**历史公开适配基线**；已由 2026-09-03 发布的 v0.8.0 公开适配修订版取代，本节仅保留历史变化记录。

### 现行法公开适配

- `cn-law-hub` 增加机器可读的十个主要官方来源登记，并由查询计划脚本逐源输出官方入口、权限范围和检索词。
- authority record 校验增加必备元数据字段、官方 HTTPS 主机校验、法源类型绑定、日期字段和重复链接检查。
- 历史基线的 T-05 路由为 `current-law-research → cn-law-hub`、`case-research → cn-case-hub`；2026-09-03 发布版的路由变化见本文件上方的 v0.8.0 部分。

### 公开边界

- 本版本只发布独立的公开适配层，不包含来源爬虫、第三方 MCP、商业数据库 SDK、缓存数据或私有运行时。
- 官方站点仍是外部数据来源；登录、验证码、robots 或其他访问控制受限时必须记录限制并停止，不得绕过。
- README 底部新增对 [`cn-law-hub`](https://github.com/ZongziForu/cn-law-hub) 的致谢，并明确不表示代码复制、隶属或官方背书。

### 验证

- 公开边界、公开表面、manifest、路由和全仓合成测试均通过；详细命令和结果见 [`TEST_REPORT_v0.8.0.md`](TEST_REPORT_v0.8.0.md)。

## [v0.7.0] - 2026-08-24

状态：**已发布公开预发布版**，不是稳定版。PR #17 的本地与远端发布门已通过。

### 第一方案例研究升级

- 重构 `cn-case-hub`，保持旧记录字段兼容，同时新增新案/存量案双入口、最新状态锁定、多轮检索校准、程序链、`A/B/C/D × +/±/0/-` 双轴矩阵和裁判分叉变量。
- 明确 `A-` 为高价值风险案例；不利案例不得因为方向不利而降低相关度或隐藏。
- 报告将案例研究结果转换为事实变量和证据缺口；检索数量只可描述为“检索样本支持比例”，不得包装为胜诉率。

### 第一方现行法研究

- 新增 `cn-law-hub`，处理法律、行政法规、司法解释、规章及条约的权威来源、效力、版本链、条文定位和时间适用；条约使用外交部条约数据库等官方来源并单独记录状态。
- 不再把现行法检索描述为必须依赖未捆绑第三方 Skill；本包不携带本地运行时中曾出现的第三方 crawler 实现。
- `cn-case-hub` 与 `cn-law-hub` 均加入第一方依赖边界检查；官方/专业数据库是数据来源而非安装依赖。

### Litigation research handoff

- `legal-os-litigation` 增加 `decision-fork variable → matter fact → evidence → evidence_gap → opponent attack → evidence action → argument/procedural action` 回流链。
- 存量案件以最新已核验程序状态为当前口径；历史状态仅保留为时间线。

### 路由与学习维护

- T-05 按 intake subtype 内部分流：`current-law-research → cn-law-hub`，`case-research → cn-case-hub`；T-01/T-04 等 substantive workflow 在研究仅为辅助时继续保持唯一 primary route。
- 将 2026-08-20 本地运行时中的 `legal-os-learning-maintenance` 作为第 14 个公开 Skill 纳入 manifest；T-12 由其负责 net-new-only 可复用改进。

### 仓库治理与安全修复

- 保留并恢复原 GitHub 的严格 manifest、schema、`execution_modes`、完整 `invocation_policy`、`template_runtime`、repository validator 和 routing validator。
- 以 GitHub `main` 的提交 `2468b9b615003fee581316503cd6da394751707a` 为 v0.7.0 发布基线；在该发布 tag 中，原 `legal-os-banner.png` 与完整历史 plan `docs/plans/2026-07-15-repository-consistency-repair.md` 均保持原字节，不以交接包中的缩略或占位版本覆盖。当前 `main` 的后续首页视觉更新另见上方 `[Unreleased]`。
- 明示修复 `LICENSE`：GitHub `main` 当时的许可证文件缺少 Apache License 2.0 正文中的一个段落；v0.7.0 将其替换为 Apache Software Foundation 官方 `LICENSE-2.0.txt` 的逐字一致文本，并以 SHA-256 `cfc7749b96f63bd31c3c42b5c471bf756814053e847c10f3eb003417bc523d30` 固定核验。该变更属于公开治理修复，不是静默替换。
- 更新 `OPEN_SOURCE_BOUNDARY.md`，明示 `cn-case-hub`、`cn-law-hub` 的第一方维护边界、外部站点仅为数据来源以及禁止绕过访问控制；本项变更同时在升级报告和 Release Notes 披露。
- 保留原 `.gitignore` 的法律数据保护：真实 DOCX/PDF/XLSX、`private/`、`confidential/`、`client-materials/`、`matter-files/` 默认不进入公开仓库。
- 恢复 24 个公开模板及 SHA-256 绑定，包括 `MEMORY-CANDIDATE-DIFF-V1.12`。
- 保留 v0.6.2 以前的架构文档、测试与 CHANGELOG 历史，不以降低验证标准换取发布通过。
- 根目录安装器适配 GitHub 源码结构，不依赖本地快照 `runtime/` 目录。
- 修复安装器对 GNU `mapfile`／`find -printf` 的隐性依赖，现可在 macOS 自带 Bash 3.2 与 BSD `find` 环境中运行；运行时安装默认调用 `python3`，也可由 `PYTHON` 显式指定。
- 删除公开脚本和规则标题中的个人称谓／内部定稿标记，重新执行公开隐私与绝对路径扫描。
- 继续执行普通和正式法律 DOCX 默认不渲染：正式提交、正式对外或“可直接使用”不单独触发视觉检查；只有明确视觉要求、视觉型交付物、复杂视觉元素或源文件检查发现具体版式风险时，才做一次定向检查。

### 验证

- 本地 repository validator、16 个 routing scenarios、24 个模板哈希、两个第一方研究边界、安装 dry-run 与回归测试全部通过。
- 全仓 `pytest` 实际执行 **93 项通过**（另有 5 个 subtests 通过）；PR #17 的项目 CI 与 CodeQL 检查通过后完成发布。
- 在独立 Python 3.12 虚拟环境中按仓库 `requirements.txt` 安装后，`pip check` 通过；远端干净 runner 仍须重新执行相同依赖门禁。

## [v0.6.2] - 2026-08-13

状态：已发布公开预发布版，非稳定版。

### Office 源文件优先门

- 在统一入口增加 `visual_check_required=false` 默认门，并使下游 DOCX/Excel 工作流继承该决定。
- 移除函件、诉讼、数据表和模板流程中残留的默认预览或全表渲染要求；普通源文件以结构、内容、修订、格式继承、公式和错误扫描为主。
- 视觉检查仅在明确要求、视觉型交付物、复杂视觉元素或具体版式缺陷时触发一次；渲染器、沙箱或缺失字体造成的环境异常不得单独阻塞结构正常的源文件。
- 增加跨工作流回归测试，防止默认渲染要求重新进入公开 Skills。

## [v0.6.1] - 2026-08-12

状态：已发布公开预发布版，非稳定版。`v0.6.1` Tag 和 GitHub 公开预发布将随本次正式流程创建。

### 法律 Word 交付规则

- 明确 DOCX 本身是正式交付物，OOXML/结构、文字、修订、样式、编号、表格和原格式保留优先。
- 将 PDF、PNG 和视觉检查限定为必要时的一次性辅助校验；中文字体、渲染器或沙箱异常在结构正确时不得单独阻塞交付。
- 保留真实源文件、隐私和授权阻塞；增加普通 120 秒、复杂 300 秒超时及一次明确原因修正性重试边界。

## [v0.6.0] - 2026-08-10

状态：已发布公开预发布版，非稳定版。

### 合同质量门

- 合同 DOCX 交付默认先做结构检查，仅在客观视觉风险触发时进行原生应用定向抽检。
- 增加极简批注规则：批注必须有正文引用，超长或明确包含多个独立事项时阻断。
- 质量门主动拒绝非法 `.//w:ins/w:p` 结构，并识别标准段落标记修订的拒绝视图。
- 保留 T-01 合同路由和既有公开 Skill 数量，不引入私有修订生成器、内部发布门或新依赖。

### 验证与边界

- 增加非法整段嵌套、标准段落标记修订、无锚点批注、超长批注和多事项批注的合成回归测试。
- 本版本已创建 `v0.6.0` Tag 和 GitHub 公开预发布；稳定版本仍需另行决定。

## [v0.5.0] - 2026-07-29

### 新增

- 为原告、申请人、债权人的起诉状、仲裁申请书、支付令申请书等首次请求文书增加单一立场门。
- 增加 R13—R16：首次请求立场控制、例外功能记录、程序功能分层和条件性表达语境复核。
- 增加案例使用控制和第三方付款条款的通用论证顺序，但不预设个案实体结论。

### 行为约束

- 对外首次请求文书不得无必要地替对方设计抗辩、证据路线、举证安排或调查计划，也不得暴露完整内部应对策略。
- 对已实际提出、请求权完整性或必要程序所需内容保留可追溯例外。
- 假设性和条件性用语只触发 `REVIEW_REQUIRED`，不构成机械禁词或自动删除。
- 从对外文书移出的风险分析继续保留在内部分析、庭审提纲、法源包或独立程序申请中。

### 验证

- 新增 6 项原告首次文书立场控制合成测试。
- 仓库一致性、15 个路由场景和 49 项自动化测试全部通过。

## [v0.4.0] - 2026-07-24

### 新增

- 为统一入口增加固定、可机读的路由输出契约和 JSON Schema。
- 增加 R0-R3 风险提问阶梯，事实、材料和法源核验问题不占用决策问题预算。
- 要求 route/blocker 字段使用稳定代码，并保留 G3 `stopped` 状态优先级。
- 增加 `cn-case-hub`，使用免费官方来源检索、核验、分级和整理中国大陆司法案例与裁判观点。

### 行为约束

- `route-only` 只返回完整路由头，不继续加载或执行主工作流。
- 外部动作增加独立授权 blocker，但不因外部动作本身将 R2 自动升级为 R3。
- blocker 必须由请求或已有材料支持，不能仅因文书类别自动推断。
- 案例搜索摘要只作为线索；正式案例记录必须打开并核验官方来源，受登录或验证码限制时停止而不绕过访问控制。
- 法规、规章、司法解释和具体法条仍由外部权威现行法检索能力核验；未捆绑许可证不明的第三方法规检索代码。

### 验证

- Skill 校验、仓库一致性、15 个路由场景和 43 项自动化测试全部通过。
- `cn-case-hub` 查询生成、4 个官方案例记录及非官方来源拒绝测试通过。
- 公共边界自动扫描未发现私有路径、秘密或个人数据高风险模式。

## [v0.3.0] - 2026-07-23

### 新增

- 增加独立 `legal-quality-gate`，作为正式或高风险法律成果的跨工作流最终质量门。
- 增加基于事项 manifest、法律依据包、人工签核、合同修订报告和 SHA-256 绑定的确定性发布锁。
- 增加 `BLOCKED`、`DRAFT_ONLY`、`RELEASE` 三种发布决定及 11 个合成回归测试。

### 安全与公共边界

- 公共导出目录通过 `--public-root` 显式指定，仅允许公开级别材料进入声明的公共根目录。
- 测试使用合成主体、合成事项和临时目录，避免依赖真实客户或案件数据。
- 发布锁要求隐私检查、人工复核和文件哈希绑定，便于审计最终交付状态。

### 验证

- 仓库验证、路由场景和自动化测试共 39 项全部通过。
- 新增 11 项质量门合成回归测试，覆盖阻断、草稿和正式发布状态。

## [v0.2.0] - 2026-07-21

### 文档与下载

- README 增加版本摘要、能力范围和 GitHub Releases 下载入口。

### 新增

- 增加 `legalos.manifest.json` 及其 Schema，统一公开版本、profiles、Skills、T-routes、调用策略和质量门清单。
- 为统一入口增加 `route-only` / `route-and-run` 执行契约。
- 增加 15 个合成路由场景以及 G3、外部动作授权、混合任务覆盖测试。
- 增加 `legal-os-template-runtime`、24 个去身份化 Office 模板、哈希绑定注册表、确定性模板解析器和 DOCX 版式审计。
- 新增劳动人事争议仲裁申请书和答辩状专用模板，避免与商事仲裁模板混用；劳动仲裁模板不设置仲裁费用承担请求。
- 所有起诉状、申请书和答辩状改为与独立证据目录成对解析和生成，诉状正文不再设置独立“证据和证据来源”章节。
- 合同诉讼、函件、数据、受理、沟通、交付、汇报和事项记忆工作流统一接入模板解析；无适用模板时返回 `TEMPLATE_REQUIRED`，不得自行设计正式版式。

### 治理

- 明确 `public-generic` 与未公开的 `private-controlled` overlay 边界。
- 由验证器检查 manifest、仓库 Skill 清单、Agent 调用元数据及文档版本投影的一致性。
- 统一 Office 文件质量门：结构化检查优先，macOS Quick Look 原生预览，WPS 或组织批准的原生应用用于中文字体、分页、表格、修订显示和打印版式的定向抽检；其他渲染器须符合部署环境的授权政策。
- 增加原生 Office 质量门回归测试，阻止已停用的渲染路径和笼统旧式渲染指令重新进入公开规则或 Skills。
- 明确“固定版式、弹性正文”：模板约束页眉页脚、字体、段落、表格等版式及最低内容骨架，不构成正文内容上限；具体事实、证据、履行节点、法律后果和风险提示应按事项充分展开。

## [v0.1.1] - 2026-07-16

### 修复

- 明确九个可安装公开 Skill 与 T-01 至 T-12 路由之间的对应关系。
- 删除合同工作流中对公开包未附带脚本和参考文件的执行性引用。
- 将证据映射明确为诉讼工作流阶段，将现行法检索和组织级最终发布审批明确为外部依赖。

### 新增

- 增加仓库结构验证器及对应的合成测试。
- 增加 DOCX 修订质量门的通过、缺少修订跟踪和清洁版不一致测试。
- 增加 Python 3.12 GitHub Actions CI、依赖一致性检查和可复现安装说明。

### 安全与发布治理

- 将 `lxml` 升级到 `6.1.0`，修复默认 `iterparse()` 和 `ETCompatXMLParser()` 配置可能读取本地文件的高危 XXE 问题（CVE-2026-41066）。
- 固定第三方 GitHub Actions 的提交 SHA，并关闭 checkout 凭据持久化。
- 增加对缺失本地资源、未打包具名 Skill 依赖、不完整 Agent 元数据和内部 Agent 指令的自动拦截。

## [v0.1.0] - 2026-07-15

- 首次公开预发布，提供 Legal OS 通用法律工作流、质量控制规则和公私边界说明。

[v0.8.0]: https://github.com/384363367-dot/legal-os/compare/v0.7.0...v0.8.0
[v0.1.1]: https://github.com/384363367-dot/legal-os/compare/v0.1.0...v0.1.1
[v0.2.0]: https://github.com/384363367-dot/legal-os/compare/v0.1.1...v0.2.0
[v0.3.0]: https://github.com/384363367-dot/legal-os/compare/v0.2.0...v0.3.0
[v0.4.0]: https://github.com/384363367-dot/legal-os/compare/v0.3.0...v0.4.0
[v0.5.0]: https://github.com/384363367-dot/legal-os/compare/v0.4.0...v0.5.0
[v0.6.0]: https://github.com/384363367-dot/legal-os/compare/v0.5.0...v0.6.0
[v0.6.2]: https://github.com/384363367-dot/legal-os/compare/v0.6.1...v0.6.2
[v0.6.1]: https://github.com/384363367-dot/legal-os/compare/v0.6.0...v0.6.1
[v0.7.0]: https://github.com/384363367-dot/legal-os/compare/v0.6.2...v0.7.0
[v0.1.0]: https://github.com/384363367-dot/legal-os/releases/tag/v0.1.0
