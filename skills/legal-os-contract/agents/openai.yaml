interface:
  display_name: "Legal OS Contract Workspace"
  short_description: "Review contracts and produce granular DOCX redlines"
  default_prompt: "Use $legal-os-contract to review this contract from my side and produce a granular tracked-changes DOCX redline. Only produce a clean version when I explicitly request one."

policy:
  allow_implicit_invocation: true
