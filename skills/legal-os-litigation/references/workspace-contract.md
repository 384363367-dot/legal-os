# Workspace contract

The litigation stack is layered:

`Litigation Analysis → Evidence Workspace → Legal Research → Pleading Workspace`

Each layer consumes identified inputs and emits traceable artifacts. A later layer may not silently supply a missing earlier-layer fact. The public repository may describe schemas and controls, but private matter files remain outside the public repository.
